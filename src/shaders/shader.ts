import vertShader from "./vert.vert?raw";
import fragShader from "./frag.frag?raw";

const vertexShaderSource = vertShader;
const fragmentShaderSource = fragShader;

export { vertexShaderSource, fragmentShaderSource };
